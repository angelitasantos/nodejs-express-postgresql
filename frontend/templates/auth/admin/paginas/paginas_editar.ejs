<!DOCTYPE html>
<html lang="pt-BR">

    <head>
        <% const base = '../../../partials'; %>
        <%- include(`${base}/head`, { title: 'Editar Página' }) %>
    </head>

    <body>
        <%- include(`${base}/header`) %>

        <main class="container">
            <div id="app"></div>

            <script>
                window.csrfToken = "<%= csrfToken %>";
                window.pageData = <%- JSON.stringify(page || []) %>;
            </script>
            <script src="/js/auth/paginas/paginas_editar.js"></script>
        </main>

        <%- include(`${base}/footer`) %>
    </body>

</html>