<!DOCTYPE html>
<html lang="pt-BR">

    <head>
        <% const base = '../../../partials'; %>
        <%- include(`${base}/head`, { title: 'Páginas do Sistema' }) %>
    </head>

    <body>
        <%- include(`${base}/header`) %>

        <main class="container">
            <div id="app"></div>

            <script>
                window.csrfToken = "<%= csrfToken %>";
                window.pagesData = <%- JSON.stringify(pages || []) %>;
            </script>
            <script src="/js/auth/paginas/paginas_sistema.js"></script>
        </main>

        <%- include(`${base}/footer`) %>
    </body>

</html>