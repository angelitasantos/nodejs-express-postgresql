<!DOCTYPE html>
<html lang="pt-BR">

    <head>
        <% const base = '../../../partials'; %>
        <%- include(`${base}/head`, { title: 'Permissões' }) %>
    </head>

    <body>
        <%- include(`${base}/header`) %>

        <main class="container">
            <h1>Permissões do Sistema</h1>
            
            <div id="app"></div>

            <script>
                window.csrfToken = "<%= csrfToken %>";
                window.permissionsData = <%- JSON.stringify(permissoes || []) %>;
            </script>
            <script src="/js/auth/permissoes/permissoes.js"></script>
        </main>

        <%- include(`${base}/footer`) %>
    </body>

</html>