<!DOCTYPE html>
<html lang="pt-BR">

    <head>
        <% const base = '../../../partials'; %>
        <%- include(`${base}/head`, { title: 'Permissões do Grupo' }) %>
    </head>

    <body>
        <%- include(`${base}/header`) %>

        <main class="container">
            <h1>Permissões do Grupo: <%= group.name %></h1>
            <div id="app"></div>

            <script>
                window.csrfToken = "<%= csrfToken %>";
                window.groupId = <%= group.id %>;
                window.permissionsData = <%- JSON.stringify(permissions || []) %>;
            </script>
            <script src="/js/auth/grupos/grupos_permissoes.js"></script>
        </main>

        <%- include(`${base}/footer`) %>
    </body>

</html>