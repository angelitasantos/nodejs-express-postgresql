<!DOCTYPE html>
<html lang="pt-BR">

    <head>
        <% const base = '../../../partials'; %>
        <%- include(`${base}/head`, { title: 'Lista de Grupos' }) %>
        <meta name="csrf-token" content="<%= csrfToken %>">
    </head>

    <body>
        <%- include(`${base}/header`) %>

        <main class="container">
            <div id="app"></div>

            <script>
                const CSRF_TOKEN = "<%= csrfToken %>";
            </script>
            <script src="/js/auth/grupos/grupos.js"></script>
        </main>

        <%- include(`${base}/footer`) %>
    </body>

</html>