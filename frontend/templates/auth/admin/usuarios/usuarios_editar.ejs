<!DOCTYPE html>
<html lang="pt-BR">

    <head>
        <% const base = '../../../partials'; %>
        <%- include(`${base}/head`, { title: 'Editar Usuário' }) %>
    </head>

    <body>
        <%- include(`${base}/header`) %>

        <main class="container">
            <div id="app"></div>

            <script>
                window.csrfToken = "<%= csrfToken %>";
                window.userData = <%- JSON.stringify(user || {}) %>;
            </script>
            <script src="/js/auth/usuarios/usuarios_editar.js"></script>
        </main>

        <%- include(`${base}/footer`) %>
    </body>

</html>