<!DOCTYPE html>
<html lang="pt-BR">

    <head>
        <% const base = '../../partials'; %>
        <%- include(`${base}/head`, { title: 'Login' }) %>
        <meta name="csrf-token" content="<%= csrfToken %>">
    </head>

    <body>
        <%- include(`${base}/header`) %>

        <main class="container">
            <div id="app"></div>

            <script>
                window.csrfToken = "<%= csrfToken %>";
            </script>
            <script src="/js/auth/public/login.js"></script>
        </main>

        <%- include(`${base}/footer`) %>
    </body>

</html>